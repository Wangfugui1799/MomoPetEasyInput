# V0.1 首次开发验证记录（烧录前）

日期：2026-09-05。此记录区分电脑测试、构建与实板证据。后续烧录与连接的最新结果见 [FLASH-STATUS.md](FLASH-STATUS.md)。

## 已完成

| 项目 | 结果 | 覆盖 |
| --- | --- | --- |
| `npm test` | 10 / 10 通过 | 饱腹上限、睡眠恢复、损坏存档修复、探索冷却、协议分帧与非法事件、AI 请求边界、模拟服务成功/失败 |
| `npm run test:firmware` | 通过 | 宿主 C 编译开启全部警告并视为错误；按键抖动、短按、长按抑制短按、开机按住、计时回绕、正反编码器、相位抖动与非法跳变 |
| `npm run test:ui` | 5 / 5 通过 | 喂食、装扮刷新保存、睡眠限制、日记、本地聊天、文本转义、训练开始/取消、音乐开始/停止、手机无横向溢出、模拟串口握手/旋转/按压/断线 |
| ESP-IDF 5.5.5 / ESP32-S3 | 构建通过 | `firmware/build/momo_easyinput.bin`，内嵌项目版本 0.1.0，204720 字节，1 MB 应用分区约剩 80% |
| Mac 打包 | 通过 | Electron 40.0.0，darwin arm64，`dist/Momo-darwin-arm64/Momo.app` |
| 打包后桌面启动 | 通过 | 原生应用启动、8 个活动键渲染、Node 隔离、置顶开启/关闭；`scripts/smoke-desktop.mjs` |
| 画面检查 | 已查看截图 | `momo-desktop.png`、`momo-mobile.png`、`momo-native.png`；最终桌面高度已收紧，八键完整显示 |

最终固件 SHA-256：

```text
31d85bc8c604946f04db3ac9b553a99246d0c360f50297466ef25f2ddc086e8b
```

板级启发式扫描：1 PASS / 1 WARN / 0 FAIL。警告 `PIN_DECLARATIONS_NOT_FOUND`：扫描器未识别本工程数组形式的引脚声明，所以不能用该扫描宣称 GPIO 已自动验证。已人工将 S1–S8、编码器、GPIO8 和共享外设安全状态与技能引脚表比对。扫描结果不能替代电气或实板验证。

第一次 ESP-IDF 构建被沙箱的只读进程查询限制阻止；在获准的正常环境重跑后通过，没有修改 SDK 或绕过检查。浏览器测试同样在正常系统环境使用独立临时资料运行。

## 未执行

- 没有芯片/MAC 验身，没有烧录、擦除、复位或设备日志采集。
- 没有实板按键、旋钮每格相位数、转向、快速输入丢包、USB 断连重连观察。模拟串口测试不等于实板联动通过。
- 没有使用真实密钥调用外部 AI；后端成功/失败通过替身响应验证，真实服务的模型兼容性与费用需要配置后确认。
- 没有验证板载 LED、麦克风、扬声器、BLE 或深睡眠；本版未启用这些功能。
- 应用未做 Developer ID 签名或公证；当前只验证本机启动，不宣称可公开分发。

下一轮实体联调应先经用户授权确认目标身份及写入范围，随后再写入并分别验证正常启动握手、S1–S8、旋转/短按/长按、连接恢复。
