# 月光小窝设计验收

final result: passed

## 视觉依据与证据

- Source visual truth：`docs/moonlight/design-reference.png`，用户确认的小魅魔月光界面，1487×1058 像素。
- Implementation：`docs/moonlight/home-1487.png`，1487×1058 CSS viewport、deviceScaleFactor 1，截图 1487×1058。
- 状态：月光主题、喂食选中、曲奇选项、麦克风关闭、一次本地文字聊天。实现采用真实宠物天数、状态和现有预设回复，未把设计稿的第 12 天和示例回复伪装为真实记录。
- Full-view paired input：`docs/moonlight/comparison.png`。两张同宽源图在浏览器中等比缩到 742px 并排，用于布局比较；原始全分辨率图同时检查，未改变图片内容。
- Focused input：`docs/moonlight/comparison-detail.png`，原像素比例检查房间边缘、状态、聊天文字、头像、录音和输入区。裁切只用于比较展示，不是运行时素材。
- 响应式：`home-1280.png`、`home-850.png`、`home-390.png`；另有 `sleep.png`、`diary.png`、`settings.png`、`classic.png`。
- Electron：`native.png`，实际内容 viewport 1223×798，截图受 Mac 显示密度与全文截图影响为 2416×1950；只用于原生可用性确认，不拿其像素尺寸直接与设计稿比较。

## 比较与修正记录

1. 首轮发现 [P2] 窄屏聊天固定高度不足，底部隐私说明被裁切。修正为 520px 聊天区与更低的消息最小高度。最新 `home-390.png` 中输入和完整说明均可见，页面没有横向溢出。
2. 首轮发现 [P2] 1280×950 时房间和操作面板过高，底部活动需要过多滚动。改为随视口高度收缩房间；最新截图中八键完整显示，较矮原生窗口仍可正常纵向滚动。
3. 首轮发现 [P2] 活动标号呈现 S01–S08，且正文偏小。新增独立展示标签为 S1–S8，保留经典的原编号；增大正文、选项、活动标题和问候字体，再次截图确认。
4. 闭眼生成图没有真实透明背景。使用清醒图的相同轮廓 alpha 作为 CSS 遮罩，并检查 `sleep.png`：角色外部没有棋盘格矩形，轮廓与清醒图一致。

## 五项检查

- **字体**：沿用 Inter / 系统 / PingFang SC 字体栈；新版聊天正文 14px、主要问候 32px、活动标签 14px。长回复自然换行，没有遮挡发送和麦克风。窄屏字号适当缩小。
- **间距与布局**：左侧约六成、右侧约四成，符合确认计划；顶部工具栏、房间、状态、选项和八键层次完整。1050px 以下改为纵向。设置与日记按模态窗口滚动。
- **颜色**：紫灰背景、暖白正文、淡玫瑰色选择和主按钮，薄荷绿角色形成焦点；焦点轮廓清晰，错误与录音状态保留文字说明。
- **图片**：独立生成房间和小魅魔角色，未把整张设计稿作为可点击背景。清醒/睡眠、头像、装扮、反馈层可组合。新工具及八键图标使用 Phosphor 原始文件，经典角色和既有装扮反馈保留。
- **文案与内容**：保留现有功能名、状态、权限错误、隐私和后端说明；采用真实状态和回复。没有新增不可操作入口，也没有自动开启麦克风。

## 行为与浏览器检查

- Codex 内置浏览器实际打开并切换新版，预览留在 `http://127.0.0.1:4791/`。
- 41 项 Playwright UI 回归通过，包括经典版、语音、无线、装扮、日记、训练、音乐、草稿及回复保留、连续三轮音频与主题切换。
- 新版截图场景及 Electron 冒烟检查收集的 pageerror 均为空。
- 原生 Electron 验证实际置顶 IPC、文字聊天、喂食和双向切换；未操作实体板或真人麦克风。

## 剩余细节

- [P3] 生成的空房间与原设计图在灯光、装饰和构图细节上有差异；主要场景、角色特征和布局一致。
- [P3] 标准图标、原生旋钮与真实文字回复在细节上不完全等同于生成效果图。保留真实的确认旋钮、较长回复与隐私说明属于功能实现所需。
- [P3] 角色沿用呼吸、弹跳、装扮及效果反馈；没有增加完整动作骨骼或口型动画。

无未解决的 P0/P1/P2。实板、真人语音与完整身体动作不属于本次视觉通过的证据范围。
