import {_electron as electron} from '@playwright/test';import assert from 'node:assert/strict';import path from 'node:path';
const app=await electron.launch({executablePath:path.resolve('dist/Momo-darwin-arm64/Momo.app/Contents/MacOS/Momo'),args:['--user-data-dir=/private/tmp/momo-desktop-smoke'],timeout:30000});
try{const page=await app.firstWindow();await page.waitForSelector('.key');assert.equal(await page.locator('.key').count(),8);assert.equal(await page.evaluate(()=>typeof window.require),'undefined');await page.evaluate(()=>window.momoDesktop.setAlwaysOnTop(true));assert.equal(await app.evaluate(({BrowserWindow})=>BrowserWindow.getAllWindows()[0].isAlwaysOnTop()),true);await page.evaluate(()=>window.momoDesktop.setAlwaysOnTop(false));await page.screenshot({path:'docs/momo-native.png',fullPage:true});console.log('PASS: packaged Mac app launches, renders 8 keys, isolates Node, and toggles always-on-top')}finally{await app.close()}
